---
phase: 02-overview-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - pages/admin/data-science/index.tsx
autonomous: true
requirements:
  - OVW-02
  - OVW-03
  - OVW-04

must_haves:
  truths:
    - "System Architecture card shows a two-row pipeline diagram with colored nodes, arrow connectors, and live counts"
    - "Clickable diagram nodes (Factor Library, Calculator Engine, ComputeRun, MetricResult) navigate to correct admin pages"
    - "Section cards 3x2 grid shows Inputs, Factors, Calculations, Test Runs, Lineage, Methodology — each card navigates on click"
    - "How Impact Governance Works Collapse is closed by default and expands to show 6-step Steps walkthrough"
    - "Old quick-link cards (Calculations/Import/Governance) and Methodology Documents card are removed"
  artifacts:
    - path: "pages/admin/data-science/index.tsx"
      provides: "Complete redesigned overview page: Architecture Diagram, Section Cards 3x2, How It Works Collapse"
      contains: "DiagramWrapper"
  key_links:
    - from: "DiagramNode (Factor Library)"
      to: "/admin/data-science/constants"
      via: "Next.js Link"
      pattern: "data-science/constants"
    - from: "SECTION_CARDS array"
      to: "Card hoverable"
      via: "Link wrapping Card"
      pattern: "hoverable"
    - from: "Collapse items"
      to: "Steps direction='vertical'"
      via: "children prop"
      pattern: "direction='vertical'"
---

<objective>
Add the System Architecture Diagram, Section Cards grid, and How It Works Collapse to complete the Data Governance overview page redesign.

Purpose: Delivers the full visual redesign after Plan 01 establishes the data foundation and header section. Replaces the old quick-link cards and Methodology Documents card.
Output: `pages/admin/data-science/index.tsx` fully redesigned with all five OVW requirements satisfied: title/subtitle (01), KPI row (01), Architecture Diagram (this plan), Section Cards (this plan), How It Works (this plan).
</objective>

<execution_context>
@/Users/derekalanrowe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/derekalanrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-overview-redesign/02-CONTEXT.md
@.planning/phases/02-overview-redesign/02-RESEARCH.md
@.planning/phases/02-overview-redesign/02-01-SUMMARY.md

<interfaces>
<!-- Styled components to add (new — not in Plan 01) -->
<!-- Add these to the styled-components block at the top of the file -->

```typescript
const DiagramWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  align-items: center;
`;

const DiagramRow = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 0;
  flex-wrap: wrap;
  justify-content: center;
`;

const DiagramNode = styled.div<{ $group: 'input' | 'processing' | 'output'; $clickable?: boolean }>`
  padding: 12px 16px;
  border-radius: 8px;
  text-align: center;
  min-width: 120px;
  background: ${p =>
    p.$group === 'input'
      ? '#f0f9ff'
      : p.$group === 'processing'
      ? '#f6ffed'
      : '#fff7e6'};
  border: 1px solid ${p =>
    p.$group === 'input'
      ? '#bae0ff'
      : p.$group === 'processing'
      ? '#b7eb8f'
      : '#ffd591'};
  cursor: ${p => (p.$clickable ? 'pointer' : 'default')};
  transition: box-shadow 0.2s;
  &:hover {
    box-shadow: ${p => (p.$clickable ? '0 2px 8px rgba(0,0,0,0.12)' : 'none')};
  }
`;

const DiagramArrow = styled.div`
  padding: 0 8px;
  color: rgba(0, 0, 0, 0.25);
  font-size: 18px;
  flex-shrink: 0;
`;

const DiagramNodeTitle = styled.div`
  font-weight: 600;
  font-size: 13px;
  color: rgba(0, 0, 0, 0.85);
`;

const DiagramNodeSub = styled.div`
  font-size: 11px;
  color: rgba(0, 0, 0, 0.45);
  margin-top: 2px;
`;
```

<!-- Section card data (static array — drives 3x2 grid) -->
<!-- functionsWithoutCoverage and totalFunctions are available from stats (Plan 01) -->

```typescript
// Imports to add to @ant-design/icons:
// ApartmentOutlined or BranchesOutlined for Lineage
// BookOutlined for Methodology
// Already present: UploadOutlined, CalculatorOutlined, FunctionOutlined, BarChartOutlined

const SECTION_CARDS = [
  {
    key: 'inputs',
    icon: <UploadOutlined />,
    title: 'Inputs',
    description: 'Detect and acknowledge data quality issues across projects.',
    href: '/admin/data-science/inputs'
  },
  {
    key: 'factors',
    icon: <CalculatorOutlined />,
    title: 'Factors',
    description: 'Manage environmental constants: EPA WARM factors, utility rates, and material weights.',
    href: '/admin/data-science/constants'
  },
  {
    key: 'calculations',
    icon: <FunctionOutlined />,
    title: 'Calculations',
    description: 'Browse the auto-discovered calculator function registry and golden dataset coverage.',
    href: '/admin/data-science/calculations'
  },
  {
    key: 'test-runs',
    icon: <BarChartOutlined />,
    title: 'Test Runs',
    description: 'Run regression tests against golden datasets to verify calculation accuracy.',
    href: '/admin/data-science/test-runs'
  },
  {
    key: 'lineage',
    icon: <ApartmentOutlined />,
    title: 'Lineage',
    description: 'Trace how a metric was produced — from input data through factors to the final result.',
    href: '/admin/data-science/lineage'
  },
  {
    key: 'methodology',
    icon: <BookOutlined />,
    title: 'Methodology',
    description: 'Maintain the methodology documents and subsections that govern impact calculation standards.',
    href: '/admin/methodology'
  }
];
```

<!-- Stats available from Plan 01 (already destructured in component) -->
<!-- projectCount, factorCount, recentComputeRunCount, metricResultCount, functionsWithoutCoverage, totalFunctions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagram styled-components and System Architecture card (OVW-02)</name>
  <files>pages/admin/data-science/index.tsx</files>
  <action>
Add the five new styled-components (DiagramWrapper, DiagramRow, DiagramNode, DiagramArrow, DiagramNodeTitle, DiagramNodeSub) to the styled-components block near the top of the file, after the existing KpiTitle styled component.

Add `ApartmentOutlined` and `BookOutlined` to the @ant-design/icons import list.

In the render, after the System Health KPI row (after `</Row>`) and before any remaining old sections, insert the System Architecture card:

```tsx
{/* System Architecture */}
<Card title='System Architecture' style={{ marginTop: 24 }}>
  <DiagramWrapper>
    {/* Row 1: Data Inputs → Factor Library → Calculator Engine */}
    <DiagramRow>
      <DiagramNode $group='input'>
        <DiagramNodeTitle>Projects / RSP Data</DiagramNodeTitle>
        <DiagramNodeSub>{projectCount} projects</DiagramNodeSub>
      </DiagramNode>
      <DiagramArrow>→</DiagramArrow>
      <Link href='/admin/data-science/constants' style={{ textDecoration: 'none', color: 'inherit' }}>
        <DiagramNode $group='processing' $clickable>
          <DiagramNodeTitle>Factor Library</DiagramNodeTitle>
          <DiagramNodeSub>{factorCount} factors</DiagramNodeSub>
        </DiagramNode>
      </Link>
      <DiagramArrow>→</DiagramArrow>
      <Link href='/admin/data-science/calculations' style={{ textDecoration: 'none', color: 'inherit' }}>
        <DiagramNode $group='processing' $clickable>
          <DiagramNodeTitle>Calculator Engine</DiagramNodeTitle>
          <DiagramNodeSub>{totalFunctions} functions</DiagramNodeSub>
        </DiagramNode>
      </Link>
    </DiagramRow>
    {/* Vertical arrow between rows */}
    <div style={{ color: 'rgba(0,0,0,0.25)', fontSize: 18, lineHeight: 1 }}>↓</div>
    {/* Row 2: ComputeRun → MetricResult → Dashboards/Insights */}
    <DiagramRow>
      <Link href='/admin/data-science/runs' style={{ textDecoration: 'none', color: 'inherit' }}>
        <DiagramNode $group='processing' $clickable>
          <DiagramNodeTitle>ComputeRun</DiagramNodeTitle>
          <DiagramNodeSub>{recentComputeRunCount} runs (7d)</DiagramNodeSub>
        </DiagramNode>
      </Link>
      <DiagramArrow>→</DiagramArrow>
      <Link href='/admin/data-science/runs' style={{ textDecoration: 'none', color: 'inherit' }}>
        <DiagramNode $group='processing' $clickable>
          <DiagramNodeTitle>MetricResult</DiagramNodeTitle>
          <DiagramNodeSub>{metricResultCount.toLocaleString()} results</DiagramNodeSub>
        </DiagramNode>
      </Link>
      <DiagramArrow>→</DiagramArrow>
      <DiagramNode $group='output'>
        <DiagramNodeTitle>Dashboards / Insights</DiagramNodeTitle>
        <DiagramNodeSub>org analytics</DiagramNodeSub>
      </DiagramNode>
    </DiagramRow>
  </DiagramWrapper>
</Card>
```

The two DiagramRows represent the two pipeline layers. The vertical arrow (↓) between rows visually connects them. Projects/RSP Data and Dashboards/Insights nodes are non-clickable (no Link wrapper, no $clickable prop). All four processing nodes link to their admin pages.
  </action>
  <verify>
    <automated>cd /Users/derekalanrowe/Dev/ChartReuse && yarn tsc --noEmit --project tsconfig.json 2>&1 | grep "data-science/index" | head -20</automated>
  </verify>
  <done>No TypeScript errors. System Architecture card renders with DiagramWrapper, two DiagramRows, arrow connectors, colored nodes, and live counts. Factor Library, Calculator Engine, ComputeRun, MetricResult nodes are clickable Links.</done>
</task>

<task type="auto">
  <name>Task 2: Replace old sections with Section Cards 3x2 grid and How It Works Collapse (OVW-03, OVW-04)</name>
  <files>pages/admin/data-science/index.tsx</files>
  <action>
**Remove old sections entirely:**
- Delete the quick-links Row (3-col Calculations/Import/Governance cards)
- Delete the Methodology Documents Card (with its List and publishedSections render)
- Delete the "How to use the Data Science Admin" Collapse and its 5-step Steps

**Add SECTION_CARDS constant** at the top of the file (after imports, before styled-components or before the component — outside the component function):

```typescript
const SECTION_CARDS = [
  {
    key: 'inputs',
    icon: <UploadOutlined />,
    title: 'Inputs',
    description: 'Detect and acknowledge data quality issues across projects.',
    href: '/admin/data-science/inputs'
  },
  {
    key: 'factors',
    icon: <CalculatorOutlined />,
    title: 'Factors',
    description: 'Manage environmental constants: EPA WARM factors, utility rates, and material weights.',
    href: '/admin/data-science/constants'
  },
  {
    key: 'calculations',
    icon: <FunctionOutlined />,
    title: 'Calculations',
    description: 'Browse the auto-discovered calculator function registry and golden dataset coverage.',
    href: '/admin/data-science/calculations'
  },
  {
    key: 'test-runs',
    icon: <BarChartOutlined />,
    title: 'Test Runs',
    description: 'Run regression tests against golden datasets to verify calculation accuracy.',
    href: '/admin/data-science/test-runs'
  },
  {
    key: 'lineage',
    icon: <ApartmentOutlined />,
    title: 'Lineage',
    description: 'Trace how a metric was produced — from input data through factors to the final result.',
    href: '/admin/data-science/lineage'
  },
  {
    key: 'methodology',
    icon: <BookOutlined />,
    title: 'Methodology',
    description: 'Maintain the methodology documents and subsections that govern impact calculation standards.',
    href: '/admin/methodology'
  }
];
```

Note: SECTION_CARDS uses JSX for `icon` values, so it must be placed inside the component or typed as `React.ReactNode`. Place it inside the component body (before the return statement) to avoid module-level JSX issues.

**Section Cards grid (OVW-03):**

After the Architecture card, insert:
```tsx
{/* Section Cards */}
<div style={{ marginTop: 24 }}>
  <Title level={4} style={{ marginBottom: 16 }}>
    Governance Sections
  </Title>
  <Row gutter={[16, 16]}>
    {SECTION_CARDS.map(card => (
      <Col xs={24} sm={12} lg={8} key={card.key}>
        <Link href={card.href} style={{ display: 'block', height: '100%' }}>
          <Card hoverable style={{ height: '100%' }}>
            <div style={{ display: 'flex', alignItems: 'flex-start', gap: 12 }}>
              <div style={{ fontSize: 20, color: 'rgba(0,0,0,0.45)', flexShrink: 0, marginTop: 2 }}>
                {card.icon}
              </div>
              <div>
                <div style={{ fontWeight: 600, fontSize: 14, marginBottom: 4 }}>{card.title}</div>
                <div style={{ fontSize: 13, color: 'rgba(0,0,0,0.45)', lineHeight: 1.4 }}>
                  {card.description}
                </div>
              </div>
            </div>
            <div style={{ marginTop: 12 }}>
              <Text style={{ fontSize: 12, color: '#1890ff' }}>View →</Text>
            </div>
          </Card>
        </Link>
      </Col>
    ))}
  </Row>
</div>
```

`xs={24} sm={12} lg={8}`: 1-col mobile, 2-col tablet, 3-col desktop. 6 cards = clean 2-row desktop grid. No status badges — health state is in the KPI row above.

**How It Works Collapse (OVW-04):**

After the Section Cards div, insert:
```tsx
{/* How It Works */}
<div style={{ marginTop: 24 }}>
  <Collapse
    ghost
    style={{ background: 'white', border: '1px solid #f0f0f0', borderRadius: 8 }}
    items={[
      {
        key: 'how-it-works',
        label: (
          <span style={{ fontWeight: 600, fontSize: 15 }}>
            <QuestionCircleOutlined style={{ marginRight: 8, color: '#2bbe50' }} />
            How Impact Governance Works
          </span>
        ),
        children: (
          <div style={{ padding: '8px 8px 16px' }}>
            <Steps
              direction='vertical'
              current={-1}
              items={[
                {
                  title: (
                    <Link href='/admin/data-science/inputs'>
                      <strong>1. Validate Inputs</strong>
                    </Link>
                  ),
                  description:
                    'Run on-demand data health scans to detect issues in project data — missing states, zero-unit line items, out-of-range return rates.',
                  icon: <UploadOutlined />
                },
                {
                  title: (
                    <Link href='/admin/data-science/constants'>
                      <strong>2. Maintain Factors</strong>
                    </Link>
                  ),
                  description:
                    'Keep the Factor Library current — EPA WARM emission factors, DOE utility rates, material weights. Propose changes via the governance workflow.',
                  icon: <CalculatorOutlined />
                },
                {
                  title: (
                    <Link href='/admin/data-science/calculations'>
                      <strong>3. Verify Calculations</strong>
                    </Link>
                  ),
                  description:
                    'Browse the calculator function registry. Every function should have golden dataset coverage to detect regressions when code or factors change.',
                  icon: <FunctionOutlined />
                },
                {
                  title: (
                    <Link href='/admin/data-science/test-runs'>
                      <strong>4. Run Regression Tests</strong>
                    </Link>
                  ),
                  description:
                    'Execute test runs against golden datasets after any code or constant change. All cards at the top should show 0 issues.',
                  icon: <BarChartOutlined />
                },
                {
                  title: (
                    <Link href='/admin/data-science/lineage'>
                      <strong>5. Trace Results</strong>
                    </Link>
                  ),
                  description:
                    'Use the Lineage page to trace how a specific metric was produced — which ComputeRun, which factors, which calculator functions.',
                  icon: <ApartmentOutlined />
                },
                {
                  title: (
                    <Link href='/admin/methodology'>
                      <strong>6. Maintain Methodology</strong>
                    </Link>
                  ),
                  description:
                    'Keep methodology documents up to date. Publish subsections that explain the scientific basis for each impact metric.',
                  icon: <BookOutlined />
                }
              ]}
            />
          </div>
        )
      }
    ]}
  />
</div>
```

The Collapse has no `defaultActiveKey` — it starts closed per the locked decision.

**Cleanup:** Remove `CodeOutlined` and `ImportOutlined` from @ant-design/icons imports if no longer used (they were in the old quick-link cards). Remove `Tag` from antd imports if unused. Keep `QuestionCircleOutlined` (used in How It Works label).

After all changes, the final render order in `<div style={{ padding: '24px' }}>` is:
1. Title + Subtitle
2. System Health KPI row (Row × 4)
3. System Architecture Card
4. Section Cards (div > Title + Row × 6)
5. How It Works (div > Collapse > Steps)
  </action>
  <verify>
    <automated>cd /Users/derekalanrowe/Dev/ChartReuse && yarn tsc --noEmit --project tsconfig.json 2>&1 | grep "data-science/index" | head -20</automated>
  </verify>
  <done>No TypeScript errors. Old quick-link cards and Methodology Documents card are gone. Section Cards 3x2 grid renders with 6 cards (Inputs, Factors, Calculations, Test Runs, Lineage, Methodology). How It Works Collapse renders with 6-step Steps, closed by default. Full render order matches: Title → KPI row → Architecture → Section Cards → How It Works.</done>
</task>

</tasks>

<verification>
Full TypeScript check after both tasks:

```bash
cd /Users/derekalanrowe/Dev/ChartReuse && yarn tsc --noEmit 2>&1 | head -40
```

Verify the complete page structure by reading the final file and checking:
- No unused imports remain (Tag, List, CodeOutlined, ImportOutlined, FileTextOutlined removed)
- publishedSections does not appear anywhere in the file
- SECTION_CARDS has exactly 6 entries (no AI Data Uploader — per locked decision)
- Collapse has no defaultActiveKey (starts closed)
- DiagramWrapper, DiagramRow, DiagramNode, DiagramArrow styled components all present
</verification>

<success_criteria>
- `pages/admin/data-science/index.tsx` has no TypeScript errors
- All 5 OVW requirements satisfied in the single file
- Page layout top-to-bottom: Title/subtitle → System Health KPI row → System Architecture card → Section Cards → How It Works Collapse
- Architecture diagram: 2-row CSS flex, color-grouped nodes (input=blue, processing=green, output=amber), clickable nodes for Factor Library/Calculator Engine/ComputeRun/MetricResult, live counts in subtitles
- Section cards: exactly 6 cards (no AI Data Uploader), 3-col desktop grid, each card navigates on click, no status badges
- How It Works Collapse: closed by default, label "How Impact Governance Works", 6 steps with Links
- No unused imports
- All deferred ideas absent: no changelog card, no AI Data Uploader section card
</success_criteria>

<output>
After completion, create `.planning/phases/02-overview-redesign/02-02-SUMMARY.md`
</output>
