---
phase: 01-navigation-and-labels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/admin/data-science/constants/index.tsx
  - pages/admin/data-science/constants/new.tsx
  - pages/admin/data-science/constants/[id]/edit.tsx
  - pages/admin/data-science/import/index.tsx
  - pages/admin/data-science/lineage/index.tsx
  - pages/admin/data-science/pipeline/index.tsx
autonomous: true
requirements:
  - LBL-01
  - LBL-02
  - LBL-03

must_haves:
  truths:
    - "The Constants Library page (constants/index.tsx) shows title 'Factors' in the browser tab and h2 heading"
    - "The Import page (import/index.tsx) shows title 'AI Data Uploader' in the browser tab and h2 heading"
    - "The Lineage page description uses governance framing about tracing how a metric was produced from raw inputs"
    - "Back-button links in new.tsx and edit.tsx say 'Back to Factors' instead of 'Back to Constants Library'"
    - "The pipeline/index.tsx selectedMenuItem is updated to a key not shown as primary nav"
  artifacts:
    - path: "pages/admin/data-science/constants/index.tsx"
      provides: "Constants page with 'Factors' title"
      contains: "Factors"
    - path: "pages/admin/data-science/import/index.tsx"
      provides: "Import page with 'AI Data Uploader' title"
      contains: "AI Data Uploader"
    - path: "pages/admin/data-science/lineage/index.tsx"
      provides: "Lineage page with governance framing description"
      contains: "trace how a metric was produced"
  key_links:
    - from: "pages/admin/data-science/constants/index.tsx"
      to: "AdminLayout title prop"
      via: "title='Factors'"
      pattern: "title='Factors'"
    - from: "pages/admin/data-science/import/index.tsx"
      to: "AdminLayout title prop"
      via: "title='AI Data Uploader'"
      pattern: "title='AI Data Uploader'"
---

<objective>
Update page titles, headings, and descriptions across six pages to match the new Data Governance naming. No routing or logic changes — label/copy updates only.

Purpose: Page titles and descriptions reflect "Data Governance" framing consistently throughout the admin.
Output: Six updated page files with correct titles, headings, back-link text, and descriptions.
</objective>

<execution_context>
@/Users/derekalanrowe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/derekalanrowe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current strings to find and replace in each file. Executor uses these directly. -->

**pages/admin/data-science/constants/index.tsx**
- Line 212: `<AdminLayout title='Constants Library' selectedMenuItem='data-science/constants' user={user}>`
  → Change `title='Constants Library'` to `title='Factors'`
- Line 216: `<Title level={2}>Constants Library</Title>`
  → Change to `<Title level={2}>Factors</Title>`
- Line 217-220: `<Paragraph>Industry-standard factors and constants used in calculations, with full versioning and governance. All values are sourced from EPA WARM, DOE, and other authoritative sources.</Paragraph>`
  → Change to: `<Paragraph>Environmental and operational factors governing impact calculations, with full versioning and audit trail. All values are sourced from EPA WARM, DOE, and other authoritative references.</Paragraph>`

**pages/admin/data-science/constants/new.tsx**
- Line 50: `<AdminLayout title='Add Factor' selectedMenuItem='data-science/constants' user={user}>`
  → No change needed (title is already fine)
- Line 54-56: Back-button text `Back to Constants Library`
  → Change to `Back to Factors`

**pages/admin/data-science/constants/[id]/edit.tsx**
- Line 107: `<AdminLayout title={\`Edit: \${factor.name}\`} selectedMenuItem='data-science/constants' user={user}>`
  → No change needed (title is already fine)
- Line 111-113: Back-button text `Back to Constants Library`
  → Change to `Back to Factors`

**pages/admin/data-science/import/index.tsx**
- Line 720: `<AdminLayout {...(pageProps as any)} selectedMenuItem='data-science/import' title='Import Data'>`
  → Change `title='Import Data'` to `title='AI Data Uploader'`
- Line 426-428: `<Title level={2} style={{ margin: 0 }}>Import Data</Title>`
  → Change to `<Title level={2} style={{ margin: 0 }}>AI Data Uploader</Title>`
- Line 429-431: `<Paragraph type='secondary' ...>AI-powered spreadsheet importer. Track upload history, classification accuracy, and column mapping patterns.</Paragraph>`
  → Change to: `<Paragraph type='secondary' style={{ marginBottom: 0, marginTop: 4 }}>AI-powered spreadsheet importer for bulk data onboarding. Track upload history, classification accuracy, and column mapping patterns.</Paragraph>`

**pages/admin/data-science/lineage/index.tsx**
- Line 321: `<AdminLayout title='Data Lineage' selectedMenuItem='data-science/lineage' user={user}>`
  → No change needed (selectedMenuItem is already 'data-science/lineage' — correct)
- Line 323-324: `<Title level={2} style={{ marginBottom: 4 }}>Data Lineage</Title>` — no change
- Lines 326-329: Current description: `End-to-end traceability from source factors (EPA WARM, DOE EIA) through the calculator engine to output metrics. Each row shows exactly which TypeScript function and output property a factor influences.`
  → Change to: `Trace how a metric was produced from raw inputs through factors and calculations to output results. Each row shows exactly which TypeScript function and output property a factor influences.`

**pages/admin/data-science/pipeline/index.tsx**
- Line 369: `<AdminLayout {...(pageProps as any)} selectedMenuItem='data-science/pipeline' title='Pipeline Traceability'>`
  → Change `selectedMenuItem='data-science/pipeline'` to `selectedMenuItem='data-science/pipeline-legacy'`
  (This key is intentionally not in the primary nav. The page still works; it just won't highlight a primary nav item. The DATA_SCIENCE_KEYS array in AdminLayout still contains 'data-science/pipeline' via the pipeline key that remains in DATA_SCIENCE_KEYS, so the group stays open — but no primary item highlights, which is the correct behavior for a legacy/unlisted page.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Constants pages — titles, headings, and back-link text</name>
  <files>
    pages/admin/data-science/constants/index.tsx,
    pages/admin/data-science/constants/new.tsx,
    pages/admin/data-science/constants/[id]/edit.tsx
  </files>
  <action>
Make the following targeted string replacements:

**In `pages/admin/data-science/constants/index.tsx`:**

1. Find: `<AdminLayout title='Constants Library' selectedMenuItem='data-science/constants' user={user}>`
   Replace: `<AdminLayout title='Factors' selectedMenuItem='data-science/constants' user={user}>`

2. Find: `<Title level={2}>Constants Library</Title>`
   Replace: `<Title level={2}>Factors</Title>`

3. Find the Paragraph immediately after that Title (the description starting with "Industry-standard factors"):
   ```
   <Paragraph>
     Industry-standard factors and constants used in calculations, with full versioning and governance. All
     values are sourced from EPA WARM, DOE, and other authoritative sources.
   </Paragraph>
   ```
   Replace with:
   ```
   <Paragraph>
     Environmental and operational factors governing impact calculations, with full versioning and audit trail. All
     values are sourced from EPA WARM, DOE, and other authoritative references.
   </Paragraph>
   ```

**In `pages/admin/data-science/constants/new.tsx`:**

4. Find: `Back to Constants Library` (inside the Button label)
   Replace: `Back to Factors`

**In `pages/admin/data-science/constants/[id]/edit.tsx`:**

5. Find: `Back to Constants Library` (inside the Button label — appears twice: once in the back-link at the top, once in the Cancel button's Link href context)
   Replace both occurrences with: `Back to Factors`

   Note: Only the Button text needs changing — the `href` props pointing to `/admin/data-science/constants` are correct and must NOT be changed.
  </action>
  <verify>
    <automated>cd /Users/derekalanrowe/Dev/ChartReuse && yarn tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- `constants/index.tsx` AdminLayout title prop reads `'Factors'`
- H2 heading in constants/index.tsx reads "Factors"
- Description paragraph updated to governance framing
- Back-button text in new.tsx reads "Back to Factors"
- Back-button text in [id]/edit.tsx reads "Back to Factors"
- No TypeScript errors introduced
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Import, Lineage, and Pipeline pages</name>
  <files>
    pages/admin/data-science/import/index.tsx,
    pages/admin/data-science/lineage/index.tsx,
    pages/admin/data-science/pipeline/index.tsx
  </files>
  <action>
Make the following targeted string replacements:

**In `pages/admin/data-science/import/index.tsx`:**

1. Find (in getLayout at line ~720):
   `<AdminLayout {...(pageProps as any)} selectedMenuItem='data-science/import' title='Import Data'>`
   Replace: `<AdminLayout {...(pageProps as any)} selectedMenuItem='data-science/import' title='AI Data Uploader'>`

2. Find (in JSX around line 426):
   `<Title level={2} style={{ margin: 0 }}>
     Import Data
   </Title>`
   Replace:
   `<Title level={2} style={{ margin: 0 }}>
     AI Data Uploader
   </Title>`

3. Find (description Paragraph):
   `AI-powered spreadsheet importer. Track upload history, classification accuracy, and column mapping patterns.`
   Replace:
   `AI-powered spreadsheet importer for bulk data onboarding. Track upload history, classification accuracy, and column mapping patterns.`

**In `pages/admin/data-science/lineage/index.tsx`:**

4. Find (description Paragraph around line 326-329):
   `End-to-end traceability from source factors (EPA WARM, DOE EIA) through the calculator engine to output
          metrics. Each row shows exactly which TypeScript function and output property a factor influences.`
   Replace:
   `Trace how a metric was produced from raw inputs through factors and calculations to output results. Each row shows
          exactly which TypeScript function and output property a factor influences.`

**In `pages/admin/data-science/pipeline/index.tsx`:**

5. Find (in getLayout at line ~369):
   `selectedMenuItem='data-science/pipeline'`
   Replace:
   `selectedMenuItem='data-science/pipeline-legacy'`

   This change ensures the pipeline page no longer highlights any primary nav item (it's a legacy/unlisted page now reachable only via direct URL or internal links). The page content and functionality are preserved as-is.
  </action>
  <verify>
    <automated>cd /Users/derekalanrowe/Dev/ChartReuse && yarn tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- import/index.tsx browser tab title reads "AI Data Uploader" (title prop)
- H2 heading in import/index.tsx reads "AI Data Uploader"
- Import description updated with "bulk data onboarding" phrasing
- lineage/index.tsx description reads "Trace how a metric was produced from raw inputs through factors and calculations to output results."
- pipeline/index.tsx selectedMenuItem changed to 'data-science/pipeline-legacy'
- No TypeScript errors introduced
  </done>
</task>

</tasks>

<verification>
After execution:
1. Visit `/admin/data-science/constants` — browser tab reads "Factors", h2 reads "Factors", description uses governance framing
2. Visit `/admin/data-science/constants/new` — back button reads "Back to Factors"
3. Visit any `/admin/data-science/constants/[id]/edit` — back button reads "Back to Factors"
4. Visit `/admin/data-science/import` — browser tab reads "AI Data Uploader", h2 reads "AI Data Uploader"
5. Visit `/admin/data-science/lineage` — description includes "Trace how a metric was produced from raw inputs"
6. Visit `/admin/data-science/pipeline` — page loads without error, no primary nav item highlighted
</verification>

<success_criteria>
- All six files updated with correct labels/descriptions
- No TypeScript compilation errors
- "Constants Library" string no longer appears in page titles or headings
- "Import Data" string no longer appears in page title or heading
- Lineage page description contains governance framing
</success_criteria>

<output>
After completion, create `.planning/phases/01-navigation-and-labels/01-02-SUMMARY.md` using the summary template.
</output>
